From 2594c2c45c59c2bfd81936fa006b1359be67afdc Mon Sep 17 00:00:00 2001
From: Emmanuel SARPEDON <emmanuel.sarpedon@appines.fr>
Date: Mon, 4 Aug 2025 16:09:16 +0400
Subject: [PATCH] refactor(fetch): enable Next.js built-in cache with Redis and
 remove redis-cache implementation

---
 cache-handler.mjs                             | 241 ++++++++++++++++++
 .../utils/http/serverSideFetch.test.ts        |  69 -----
 src/__tests__/utils/http/strapiFetch.test.ts  |  29 ---
 src/app/api/revalidate/[tag]/route.ts         |   9 +
 src/constants/endpoints.ts                    |   3 +-
 src/types/http.d.ts                           |   1 -
 src/utils/http/serverSideFetch.ts             | 197 +++-----------
 src/utils/redis-cache/index.ts                |  47 ----
 src/utils/services/appines/index.ts           |   9 -
 9 files changed, 293 insertions(+), 312 deletions(-)
 create mode 100644 cache-handler.mjs
 create mode 100644 src/app/api/revalidate/[tag]/route.ts
 delete mode 100644 src/utils/redis-cache/index.ts

diff --git a/cache-handler.mjs b/cache-handler.mjs
new file mode 100644
index 00000000..72877099
--- /dev/null
+++ b/cache-handler.mjs
@@ -0,0 +1,241 @@
+import { CacheHandler } from "@neshca/cache-handler";
+import { isImplicitTag } from "@neshca/cache-handler/helpers";
+import pkg from "redis";
+
+CacheHandler.onCreation(async () => {
+  const { createClient, commandOptions } = pkg;
+
+  // Always create a Redis client inside the `onCreation` callback.
+  const client = createClient({
+    url:
+      process.env.NODE_ENV === "development" || process.env.APP_ENV === "development"
+        ? "redis://localhost:6378"
+        : `redis://:${process.env.REDIS_PASSWORD}@${process.env.REDIS_HOST}:${process.env.REDIS_PORT}`,
+  });
+
+  // Redis won't work without error handling. https://github.com/redis/node-redis?tab=readme-ov-file#events
+  client.on("error", error => {
+    if (typeof process.env.NEXT_PRIVATE_DEBUG_CACHE !== "undefined") {
+      // Use logging with caution in production. Redis will flood your logs. Hide it behind a flag.
+      console.error("Redis client error:", error);
+    }
+  });
+
+  await client.connect();
+
+  // Define a timeout for Redis operations.
+  const timeoutMs = 1000;
+
+  // Define a key prefix for the cache.
+  // It is useful to avoid key collisions with other data in Redis,
+  // or to delete all cache keys at once by using a pattern.
+  const keyPrefix = `nextjs-website-${process.env.APP_ENV || process.env.NODE_ENV}:`;
+
+  // Define a key for shared tags.
+  // You'll see how to use it later in the `revalidateTag` method
+  const sharedTagsKey = "_sharedTags_";
+
+  // Create an assert function to ensure that the client is ready before using it.
+  // When you throw an error in any Handler method,
+  // the CacheHandler will use the next available Handler listed in the `handlers` array.
+  function assertClientIsReady() {
+    if (!client.isReady) {
+      throw new Error("Redis client is not ready yet or connection is lost.");
+    }
+  }
+
+  const revalidatedTagsKey = `${keyPrefix}__revalidated_tags__`;
+
+  // Create a custom Redis Handler
+  const customRedisHandler = {
+    // Give the handler a name.
+    // It is useful for logging in debug mode.
+    name: "redis-strings-custom",
+    // We do not use try/catch blocks in the Handler methods.
+    // CacheHandler will handle errors and use the next available Handler.
+    async get(key, { implicitTags }) {
+      // Ensure that the client is ready before using it.
+      // If the client is not ready, the CacheHandler will use the next available Handler.
+      assertClientIsReady();
+
+      // Create a new AbortSignal with a timeout for the Redis operation.
+      // By default, redis client operations will wait indefinitely.
+      const options = commandOptions({
+        signal: AbortSignal.timeout(timeoutMs),
+      });
+
+      // Get the value from Redis.
+      // We use the key prefix to avoid key collisions with other data in Redis.
+      const result = await client.get(options, keyPrefix + key);
+
+      // If the key does not exist, return null.
+      if (!result) {
+        return null;
+      }
+
+      // Redis stores strings, so we need to parse the JSON.
+      const cacheValue = JSON.parse(result);
+
+      // If the cache value has no tags, return it early.
+      if (!cacheValue) {
+        return null;
+      }
+
+      // Get the set of explicit and implicit tags.
+      // implicitTags are available only on the `get` method.
+      const combinedTags = new Set([...cacheValue.tags, ...implicitTags]);
+
+      // If there are no tags, return the cache value early.
+      if (combinedTags.size === 0) {
+        return cacheValue;
+      }
+
+      // Get the revalidation times for the tags.
+      const revalidationTimes = await client.hmGet(
+        commandOptions({ signal: AbortSignal.timeout(timeoutMs) }),
+        revalidatedTagsKey,
+        Array.from(combinedTags)
+      );
+
+      // Iterate over all revalidation times.
+      for (const timeString of revalidationTimes) {
+        // If the revalidation time is greater than the last modified time of the cache value,
+        if (timeString && Number.parseInt(timeString, 10) > cacheValue.lastModified) {
+          // Delete the key from Redis.
+          await client.unlink(commandOptions({ signal: AbortSignal.timeout(timeoutMs) }), keyPrefix + key);
+
+          // Return null to indicate cache miss.
+          return null;
+        }
+      }
+
+      // Return the cache value.
+      return cacheValue;
+    },
+    async set(key, cacheHandlerValue) {
+      // Ensure that the client is ready before using it.
+      assertClientIsReady();
+
+      // Create a new AbortSignal with a timeout for the Redis operation.
+      const options = commandOptions({
+        signal: AbortSignal.timeout(timeoutMs),
+      });
+
+      // Redis stores strings, so we need to stringify the JSON.
+      const setOperation = client.set(options, keyPrefix + key, JSON.stringify(cacheHandlerValue));
+
+      // If the cacheHandlerValue has a lifespan, set the automatic expiration.
+      // cacheHandlerValue.lifespan can be null if the value is the page from the Pages Router without getStaticPaths or with `fallback: false`
+      // so, we need to check if it exists before using it
+      const expireOperation = cacheHandlerValue.lifespan
+        ? client.expireAt(options, keyPrefix + key, cacheHandlerValue.lifespan.expireAt)
+        : undefined;
+
+      // If the cache handler value has tags, set the tags.
+      // We store them separately to save time to retrieve them in the `revalidateTag` method.
+      const setTagsOperation = cacheHandlerValue.tags.length
+        ? client.hSet(options, keyPrefix + sharedTagsKey, key, JSON.stringify(cacheHandlerValue.tags))
+        : undefined;
+
+      // Wait for all operations to complete.
+      await Promise.all([setOperation, expireOperation, setTagsOperation]);
+    },
+    async revalidateTag(tag) {
+      // Ensure that the client is ready before using it.
+      assertClientIsReady();
+
+      // Check if the tag is implicit.
+      // Implicit tags are not stored in the cached values.
+      if (isImplicitTag(tag)) {
+        // Mark the tag as revalidated at the current time.
+        await client.hSet(
+          commandOptions({ signal: AbortSignal.timeout(timeoutMs) }),
+          revalidatedTagsKey,
+          tag,
+          Date.now()
+        );
+      }
+
+      // Create a map to store the tags for each key.
+      const tagsMap = new Map();
+
+      // Cursor for the hScan operation.
+      let cursor = 0;
+
+      // Define a query size for the hScan operation.
+      const hScanOptions = { COUNT: 100 };
+
+      // Iterate over all keys in the shared tags.
+      do {
+        const remoteTagsPortion = await client.hScan(
+          commandOptions({ signal: AbortSignal.timeout(timeoutMs) }),
+          keyPrefix + sharedTagsKey,
+          cursor,
+          hScanOptions
+        );
+
+        // Iterate over all keys in the portion.
+        for (const { field, value } of remoteTagsPortion.tuples) {
+          // Parse the tags from the value.
+          tagsMap.set(field, JSON.parse(value));
+        }
+
+        // Update the cursor for the next iteration.
+        cursor = remoteTagsPortion.cursor;
+
+        // If the cursor is 0, we have reached the end.
+      } while (cursor !== 0);
+
+      // Create an array of keys to delete.
+      const keysToDelete = [];
+
+      // Create an array of tags to delete from the hash map.
+      const tagsToDelete = [];
+
+      // Iterate over all keys and tags.
+      for (const [key, tags] of tagsMap) {
+        // If the tags include the specified tag, add the key to the delete list.
+        if (tags.includes(tag)) {
+          // Key must be prefixed because we use the key prefix in the set method.
+          keysToDelete.push(keyPrefix + key);
+          // Set an empty string as the value for the revalidated tag.
+          tagsToDelete.push(key);
+        }
+      }
+
+      // If there are no keys to delete, return early.
+      if (keysToDelete.length === 0) {
+        return;
+      }
+
+      // Delete the keys from Redis.
+      const deleteKeysOperation = client.unlink(
+        commandOptions({ signal: AbortSignal.timeout(timeoutMs) }),
+        keysToDelete
+      );
+
+      // Update the tags in Redis by deleting the revalidated tags.
+      const updateTagsOperation = client.hDel(
+        // Use the isolated option to prevent the command from being executed on the main connection.
+        {
+          isolated: true,
+          ...commandOptions({ signal: AbortSignal.timeout(timeoutMs) }),
+        },
+        keyPrefix + sharedTagsKey,
+        tagsToDelete
+      );
+
+      // Wait for all operations to complete.
+      await Promise.all([deleteKeysOperation, updateTagsOperation]);
+    },
+  };
+
+  return {
+    // The order of the handlers is important.
+    // The CacheHandler will run get methods in the order of the handlers array.
+    // Other methods will be run in parallel.
+    handlers: [customRedisHandler],
+  };
+});
+
+export default CacheHandler;
diff --git a/src/__tests__/utils/http/serverSideFetch.test.ts b/src/__tests__/utils/http/serverSideFetch.test.ts
index 4a52345c..cb611d9c 100644
--- a/src/__tests__/utils/http/serverSideFetch.test.ts
+++ b/src/__tests__/utils/http/serverSideFetch.test.ts
@@ -1,80 +1,11 @@
 import { serverSideFetch } from "@/utils/http/serverSideFetch";
-import config from "@/config/index";
-import redisInstance from "@/utils/redis-cache";
-import { Mock } from "jest-mock";
 import { simulateServerEnvironment } from "@/utils/tests";
 
-jest.mock("@/utils/redis-cache");
-
 describe("serverSideFetch", () => {
   simulateServerEnvironment();
 
-  const validUrlFromOurApi = "https://valid-url-from-our-api.com";
-
   it("should throw an error if the URL is invalid", async () => {
     const url = "invalid-url";
     await expect(serverSideFetch(url)).rejects.toThrowError();
   });
-
-  it("should return data from cache if the request is cacheable and in the cache", async () => {
-    config.api.url = validUrlFromOurApi;
-    (redisInstance.getJSON as Mock).mockReturnValueOnce({ data: "Awesome data from amazing cache" });
-    const url = new URL("/foo/bar", validUrlFromOurApi).toString();
-
-    expect(await serverSideFetch(url, { method: "POST" })).toEqual({ data: "Awesome data from amazing cache" });
-  });
-
-  it("should put data in the cache if the request is cacheable and not in the cache if content-type = application/json", async () => {
-    config.api.url = validUrlFromOurApi;
-    (redisInstance.getJSON as Mock).mockReturnValueOnce(undefined);
-    redisInstance.setJSON = jest.fn();
-
-    const url = new URL("/foo/bar", validUrlFromOurApi).toString();
-
-    global.fetch = jest.fn(() =>
-      Promise.resolve({
-        statusText: "OK",
-        status: 200,
-        ok: true,
-        headers: {
-          get: (name: string): string | undefined => {
-            if (name === "content-length") return "100";
-            if (name === "content-type") return "application/json";
-          },
-        },
-        json: () => Promise.resolve({ data: "Awesome data from amazing API" }),
-      } as unknown as Promise<Response>)
-    );
-
-    await serverSideFetch(url, { method: "POST" });
-
-    expect(redisInstance.setJSON).toHaveBeenCalledTimes(1);
-  });
-
-  it("should put data in the cache if the request is cacheable and not in the cache if content-type = text/plain", async () => {
-    config.api.url = validUrlFromOurApi;
-    (redisInstance.getJSON as Mock).mockReturnValueOnce(undefined);
-    redisInstance.setJSON = jest.fn();
-
-    const url = new URL("/foo/bar", validUrlFromOurApi).toString();
-
-    global.fetch = jest.fn(() =>
-      Promise.resolve({
-        statusText: "OK",
-        status: 200,
-        ok: true,
-        headers: {
-          get: (name: string): string | undefined => {
-            if (name === "content-length") return "100";
-            if (name === "content-type") return "text/plain";
-          },
-        },
-        text: () => Promise.resolve("Awesome data from amazing API"),
-      } as unknown as Promise<Response>)
-    );
-
-    await serverSideFetch(url, { method: "POST" });
-
-    expect(redisInstance.setJSON).toHaveBeenCalledTimes(1);
-  });
 });
diff --git a/src/__tests__/utils/http/strapiFetch.test.ts b/src/__tests__/utils/http/strapiFetch.test.ts
index ec070dab..d0852548 100644
--- a/src/__tests__/utils/http/strapiFetch.test.ts
+++ b/src/__tests__/utils/http/strapiFetch.test.ts
@@ -2,10 +2,7 @@ import { strapiEnhancedFetch } from "@/utils/http/strapiEnhancedFetch";
 import config from "@/config/index";
 import { simulateServerEnvironment } from "@/utils/tests";
 import { STRAPI_API } from "@/constants/endpoints";
-import redisInstance from "@/utils/redis-cache";
-import { Mock } from "jest-mock";
 
-jest.mock("@/utils/redis-cache");
 describe("strapiEnhancedFetch", () => {
   simulateServerEnvironment();
 
@@ -13,30 +10,4 @@ describe("strapiEnhancedFetch", () => {
     config.strapi = undefined as any;
     await expect(() => strapiEnhancedFetch("/" as STRAPI_API)).rejects.toThrow();
   });
-
-  it("should return a response if strapi config is present", async () => {
-    config.strapi = { url: "https://example.com", jwtToken: "token", jwtTokenForComments: "token-for-comments" };
-    const expectedJSON = { data: "Awesome data from amazing Strapi API" };
-    (redisInstance.getJSON as Mock).mockReturnValueOnce(expectedJSON);
-
-    global.fetch = jest.fn(
-      () =>
-        Promise.resolve({
-          statusText: "OK",
-          status: 200,
-          ok: true,
-          headers: {
-            get: (name: string): string | undefined => {
-              if (name === "content-length") return "100";
-              if (name === "content-type") return "application/json";
-            },
-          },
-          json: () => Promise.resolve(expectedJSON),
-        }) as unknown as Promise<Response>
-    );
-
-    const response = await strapiEnhancedFetch("/api/foo" as STRAPI_API);
-
-    expect(response).toEqual(expectedJSON);
-  });
 });
diff --git a/src/app/api/revalidate/[tag]/route.ts b/src/app/api/revalidate/[tag]/route.ts
new file mode 100644
index 00000000..634d1591
--- /dev/null
+++ b/src/app/api/revalidate/[tag]/route.ts
@@ -0,0 +1,9 @@
+import { EnhancedNextResponse } from "@/src/utils/http/EnhancedNextResponse";
+import { revalidateTag } from "next/cache";
+import { NextRequest } from "next/server";
+
+export async function PUT(request: NextRequest, props: { params: Promise<{ tag: string }> }) {
+  const tag = await props.params.then(params => params.tag);
+  revalidateTag(tag);
+  return EnhancedNextResponse.accepted();
+}
diff --git a/src/constants/endpoints.ts b/src/constants/endpoints.ts
index 9de2fae8..50dabe57 100644
--- a/src/constants/endpoints.ts
+++ b/src/constants/endpoints.ts
@@ -36,7 +36,8 @@ export enum NEXT_API {
   FORM_LEAD_PARTNER                   = "/api/form/propositions/lead-for-partner",
   POST_COMMENT                        = "/api/strapi/comments",
   REMOVE_PUBLISHED_COMMENTS_TO_COOKIE = "/api/cookies/remove-published-comments",
-  SUBSCRIBE_TO_NEWSLETTER             = "/api/newsletter/subscribe"
+  SUBSCRIBE_TO_NEWSLETTER             = "/api/newsletter/subscribe",
+  REVALIDATE_TAG                      = "/api/revalidate/:tag"
 }
 // prettier-ignore
 export enum STRAPI_API {
diff --git a/src/types/http.d.ts b/src/types/http.d.ts
index 96cb600f..997843c0 100644
--- a/src/types/http.d.ts
+++ b/src/types/http.d.ts
@@ -6,6 +6,5 @@ type ServerSideFetchOptions = {
   invalidateCache?: boolean;
   updateCache?: boolean;
   doNotAbstractFetchResponse?: boolean;
-  req?: NextApiRequest | NextRequest;
   cacheDuration?: number;
 };
diff --git a/src/utils/http/serverSideFetch.ts b/src/utils/http/serverSideFetch.ts
index 60fa50d5..c45680c9 100644
--- a/src/utils/http/serverSideFetch.ts
+++ b/src/utils/http/serverSideFetch.ts
@@ -1,12 +1,10 @@
 import { ServerSideFetchOptions } from "@/types/http";
 import hash from "object-hash";
-import { logger } from "@/utils/logs";
-import * as Log from "@/utils/logs";
-import { revalidateTag } from "next/cache";
-import { getIP, isAppRouter, isResponseContentTypeJson, isValidHttpUrl } from "@/utils/http/index";
-import redisInstance from "@/utils/redis-cache";
+import { isAppRouter, isResponseContentTypeJson, isValidHttpUrl } from "@/utils/http/index";
+import { NEXT_API } from "@/src/constants/endpoints";
+import config from "@/config/index";
+import { CUSTOM_HEADERS } from "@/src/constants/headers";
 
-// prettier-ignore
 /**
  * Custom fetch function that abstracts the fetch response and caches the data. Works only on server side.
  * @param input
@@ -14,165 +12,52 @@ import redisInstance from "@/utils/redis-cache";
  * @param options
  */
 export async function serverSideFetch(input: RequestInfo, init?: RequestInit, options?: ServerSideFetchOptions) {
-  const startTimestamp: number = Date.now();
+  if (typeof window !== "undefined")
+    throw new Error("serverSideFetch: this function should be used only on server side");
+  if (!isValidHttpUrl(input.toString())) throw new Error(`serverSideFetch: Invalid URL (${input.toString()})`);
 
-  try {
-    if (typeof window !== "undefined")     throw new Error("serverSideFetch: this function should be used only on server side");
-    if (!isValidHttpUrl(input.toString())) throw new Error("Invalid URL", { cause: "serverSideFetch: input is not a valid URL" });
+  const { updateCache, invalidateCache, disableCache, doNotAbstractFetchResponse, cacheDuration } = options || {};
+  const requestHash: string = hash(JSON.stringify({ input, body: init?.body }));
 
-    const domain: URL = new URL(input.toString());
-    const cacheableInRedis: boolean = isCacheableInRedis(options);
-    const { updateCache, invalidateCache, ...restOptions } = options || {};
-    const requestHash: string = hash(JSON.stringify({ input, body: init?.body, options: { ...restOptions, req: undefined } }));
-
-    // --- REDIS CACHE HANDLER ---
-    if (invalidateCache) {
-      cacheableInRedis && redisInstance?.del(requestHash);
-      revalidateTag(requestHash);
-      return {}; // /!\ Do not return undefined, null or void
-    }
-
-    if (updateCache) {
-      cacheableInRedis && redisInstance?.del(requestHash);
-      revalidateTag(requestHash);
-    }
-    
-    if (cacheableInRedis && !updateCache && !invalidateCache) {
-      const dataInCache = await redisInstance?.getJSON(requestHash);
-
-      if (dataInCache) {
-        const endTimestamp = Date.now();
-        logSuccessCache({ endTimestamp, startTimestamp, init, domain, options, requestHash });
-        return dataInCache;
-      }
-    }
-
-    // --- BUILT-IN FETCH LOGIC ---
-    let headers = { ...init?.headers };
-    if (init?.method === "POST") headers = { ...headers, "Content-Type": "application/json" };
-
-    const cacheDuration = options?.cacheDuration || 86400; // default 24h
-
-    const res = await fetch(input, {
-      ...init,
-      headers,
-      next: { revalidate: options?.disableCache || cacheableInRedis ? 0 : cacheDuration, tags: [requestHash] },
+  if (invalidateCache || updateCache) {
+    await fetch(new URL(NEXT_API.REVALIDATE_TAG.replace(":tag", requestHash), config.origin), {
+      method: "PUT",
+      cache: "no-store",
     });
+  }
 
-    const endTimestamp = Date.now();
-    logHttp({res, startTimestamp, endTimestamp, options, isCacheable: cacheableInRedis, domain, init});
-
-    if (options?.doNotAbstractFetchResponse) return res;
+  if (invalidateCache) return Promise.resolve(new Response());
 
-    if (res.ok) {
-      if (isResponseContentTypeJson(res)) {
-        const json = await res.json();
-        // If the request is cacheable, cache the response
-        if (cacheableInRedis) redisInstance?.setJSON(requestHash, json, { cacheDuration });
-        return json;
-      }
+  let headers = { ...init?.headers };
+  if (init?.method === "POST") headers = { ...headers, "Content-Type": "application/json" };
 
-      const text = await res.text();
+  const ttl: number = (() => {
+    if (disableCache || cacheDuration === 0) return 0;
+    if (cacheDuration) return isFinite(cacheDuration) ? cacheDuration : Infinity;
+    return 86400; // default 24h
+  })();
 
-      // If the request is cacheable, cache the response
-      if (cacheableInRedis) redisInstance?.setJSON(requestHash, text, { cacheDuration });
+  const res = await fetch(input, {
+    ...init,
+    headers,
+    next: doNotAbstractFetchResponse ? undefined : { revalidate: ttl, tags: [requestHash] },
+    cache: doNotAbstractFetchResponse ? "no-store" : undefined,
+  });
 
-      return text;
-    }
-  } catch (e: any) {
-    logger.error({
-      host: typeof input === "string" ? new URL(input).host : undefined,
-      statusText: e?.message,
-      httpRequest: {
-        requestMethod: init?.method,
-        requestUrl:    typeof input === "string" ? input : undefined,
-        requestSize:   (options?.req as any)?.headers["content-length"] || (options?.req as any)?.headers.get("content-length"),
-        status:        500,
-        userAgent:     (options?.req as any)?.headers["user-agent"] || (options?.req as any)?.headers.get("user-agent"),
-        remoteIp:      getIP(options?.req),
-        referer:       (options?.req as any)?.headers.referer || (options?.req as any)?.headers.get("referer"),
-        latency:       `${(Date.now() - startTimestamp) / 1000}s`,
-      },
+  if (isAppRouter()) {
+    import("next/server").then(({ after }) => {
+      after(async () => {
+        if (process.env.NEXT_RUNTIME !== "edge") {
+          const { logger } = await import("@/utils/logs");
+          const { headers } = await import("next/headers");
+          const operationId = await headers().then(headers => headers.get(CUSTOM_HEADERS.LOGGING_OPERATION_ID));
+          logger.info({ operation: { id: operationId }, status: res.status }, String(input));
+        }
+      });
     });
   }
-}
 
-function isCacheableInRedis(options?: ServerSideFetchOptions): boolean {
-  if (options?.disableCache) return false;
-  if (options?.doNotAbstractFetchResponse) return false;
-  return true;
+  if (doNotAbstractFetchResponse) return res;
+  if (isResponseContentTypeJson(res)) return await res.json();
+  return await res.text();
 }
-
-const logSuccessCache = ({
-  startTimestamp,
-  endTimestamp,
-  domain,
-  options,
-  init,
-  requestHash,
-}: {
-  startTimestamp: number;
-  endTimestamp: number;
-  domain: URL;
-  options?: ServerSideFetchOptions;
-  init?: RequestInit;
-  requestHash: string;
-}): void => {
-  const latency = endTimestamp - startTimestamp;
-  const isHighLatency = latency > 2000;
-  const ip = getIP(options?.req);
-  const message = () => {
-    if (isHighLatency) return `304(high latency cache) - ${domain.pathname}`;
-    return `304(cache) - ${domain.pathname}`;
-  };
-
-  logger[isHighLatency ? "warn" : "trace"]({
-    ...Log.buildBody({
-      req: options?.req,
-      dataFetchFromCache: true,
-      isCacheable: true,
-      disableCache: options?.disableCache,
-      domain,
-      startTimestamp,
-      ip,
-      init,
-      endTimestamp,
-    }),
-    message: message(),
-    requestHash,
-  });
-};
-
-const logHttp = ({
-  res,
-  startTimestamp,
-  endTimestamp,
-  options,
-  isCacheable,
-  domain,
-  init,
-}: {
-  res: Response;
-  startTimestamp: number;
-  endTimestamp: number;
-  options?: ServerSideFetchOptions;
-  isCacheable: boolean;
-  domain: URL;
-  init?: RequestInit;
-}): void => {
-  const ip = getIP(options?.req);
-  logger[res.ok ? "info" : "warn"]({
-    ...Log.buildBody({
-      req: options?.req,
-      res,
-      isCacheable,
-      disableCache: options?.disableCache,
-      domain,
-      startTimestamp,
-      ip,
-      init,
-      endTimestamp,
-    }),
-    message: `${res.status} - ${domain.pathname}`,
-  });
-};
diff --git a/src/utils/redis-cache/index.ts b/src/utils/redis-cache/index.ts
deleted file mode 100644
index a0a97c29..00000000
--- a/src/utils/redis-cache/index.ts
+++ /dev/null
@@ -1,47 +0,0 @@
-import { Redis } from "ioredis";
-import config from "@/config/index";
-import { logger } from "@/utils/logs";
-import { RedisOptions } from "ioredis/built/redis/RedisOptions";
-import parseJSON from "@/utils/helpers/parseJSON";
-
-if (typeof window !== "undefined" && process.env.NODE_ENV !== "test")
-  throw new Error("redisInstance: this function should be used only on server side");
-
-// prettier-ignore
-const REDIS_OPTIONS: RedisOptions = {
-  host:                 config.redis.host,
-  port:                 config.redis.port ? +config.redis.port : undefined,
-  password:             config.redis.password,
-  enableAutoPipelining: true,
-
-  retryStrategy: times => {
-    if (times <= 20) return Math.min(times * 500, 2000); // Retry after 500ms, 1000ms, 1500ms, 2000ms, 2000ms, 2000ms, 2000ms, ...
-
-    logger.fatal("Redis connection failed");
-    return null; // Stop retrying
-  },
-}
-
-class EnhancedRedis extends Redis {
-  constructor() {
-    super({ ...REDIS_OPTIONS });
-  }
-
-  async setJSON(key: string, value: unknown, options?: { cacheDuration?: number }) {
-    const cacheDuration = options?.cacheDuration;
-    const stringValue = typeof value === "number" ? value : JSON.stringify(value); // "JSON.SET" support string | number | Buffer[]
-
-    await this.call("JSON.SET", key, "$", stringValue);
-    if (cacheDuration && isFinite(cacheDuration)) await this.expire(key, cacheDuration);
-  }
-
-  async getJSON<T>(key: string): Promise<T | null> {
-    const value = await this.call("JSON.GET", key);
-    if (!value) return null;
-
-    return parseJSON(value);
-  }
-}
-
-const enhancedRedis = new EnhancedRedis();
-export default enhancedRedis;
diff --git a/src/utils/services/appines/index.ts b/src/utils/services/appines/index.ts
index 65fcaf81..fa950e80 100644
--- a/src/utils/services/appines/index.ts
+++ b/src/utils/services/appines/index.ts
@@ -20,13 +20,6 @@ export async function getAggregateRatings(
   googlePlayScrapper: GooglePlayScraper = googlePlayScrapperModule
 ): Promise<AggregateRatings> {
 
-  // check if data is in cache
-  const hashKey = hash("getAggregatingReviews");
-  const redisInstance = require("@/utils/redis-cache").default;
-  const dataInCache = await redisInstance.getJSON(hashKey);
-
-  if (dataInCache) return dataInCache as AggregateRatings;
-
   const APPLE_APP_ID  = "1439768840";
   const GOOGLE_APP_ID = "com.appines.app";
   const COUNTRY       = "fr";
@@ -49,8 +42,6 @@ export async function getAggregateRatings(
     all:    { ratingValue: averageRatingValue, ratingCount: averageRatingCount },
   };
 
-  // save data in cache during 24h
-  redisInstance.setJSON(hashKey, ratings, { cacheDuration: 60 * 60 * 24 });
 
   return ratings;
 }
-- 
2.49.0
